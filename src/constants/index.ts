import path from 'path'

const IMAGE_DIR_PATH: string = path.resolve(__dirname, '../../images/full')
const IMAGE_OUTPUT_DIR_PATH: string = path.resolve(
  __dirname,
  '../../images/thumb'
)

export { IMAGE_DIR_PATH, IMAGE_OUTPUT_DIR_PATH }
